ID: helloworld
_schema-version: '2.1'
description: Hello World
version: 0.0.1

modules:
 - name: hello
   type: nodejs
   path: hello
   provides:
    - name: hello_api
      properties:
         url: ${default-url}
